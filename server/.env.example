# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.jjxpd.mongodb.net/?appName=Cluster0&retryWrites=true&w=majority

# JWT Secret (Generate a strong secret key)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET=your_jwt_secret_key_here

# Server Configuration
PORT=5002
NODE_ENV=development

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:5173

# Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Production Environment Variables:
# Set NODE_ENV=production
# Update CLIENT_URL to your production frontend URL
# Update MONGODB_URI with production database credentials
# Generate new JWT_SECRET for production
